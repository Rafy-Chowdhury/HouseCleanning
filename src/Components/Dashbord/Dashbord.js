import React from 'react';
import Sidebar from '../Slidebar/Sidebar';

const Dashbord = () => {
    return (
        <div>
            
            <Sidebar/>
        </div>
    );
};

export default Dashbord;